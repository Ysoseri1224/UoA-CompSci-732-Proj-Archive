// Skill selection page — each player picks 2 skills from a pool of 6.
// Selection logic, countdown timer, and socket events will be added in PR 9.
// Requires authentication (protected by PrivateRoute).
function SkillSelectPage() {
  return <h1>Skill Select Page</h1>;
}

export default SkillSelectPage;
