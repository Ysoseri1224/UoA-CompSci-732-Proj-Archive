// Profile page — displays a user's public info, stats, and achievements.
// Public route: anyone can view a profile; only the owner can edit.
// API integration will be added in PR 14.
function ProfilePage() {
  return <h1>Profile Page</h1>;
}

export default ProfilePage;
