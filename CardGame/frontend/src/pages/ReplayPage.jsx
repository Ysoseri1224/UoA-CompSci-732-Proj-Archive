// Replay page — step through a completed match hand by hand.
// Read-only; no socket connection needed.
// API integration will be added in PR 14.
// Requires authentication (protected by PrivateRoute).
function ReplayPage() {
  return <h1>Replay Page</h1>;
}

export default ReplayPage;
