// Main game page — renders the poker table driven by gameState from the socket.
// GameBoard, BettingControls, SkillPanel, and EventLog will be added in PR 10+.
// Requires authentication (protected by PrivateRoute).
function GamePage() {
  return <h1>Game Page</h1>;
}

export default GamePage;
