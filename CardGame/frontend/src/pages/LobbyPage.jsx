// Lobby page — create room, join room by code, or quick match.
// Socket event wiring will be added in PR 8.
// Requires authentication (protected by PrivateRoute).
function LobbyPage() {
  return <h1>Lobby Page</h1>;
}

export default LobbyPage;
